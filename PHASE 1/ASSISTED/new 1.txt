SELECT SAL,ROUND(sal/1000)*1000 FROM EMP

SELECT LAST_DAY(HIREDATE) FROM EMP;

SELECT TO_CHAR(SYSDATE, ‘ddth-MON-YYYY’) FROM DUAL;

SELECT AVG(SAL) FROM EMP WHERE JOB = ‘CLERK’;

SELECT MAX(SAL),DEPTNO FROM EMP GROUP BY DEPTNO;

SELECT ENAME,SAL*12 AS “ANNUAL SAL” FROM EMP;

SELECT DISTINCT(JOB) FROM EMP;

SELECT ENAME FROM EMP WHERE ENAME LIKE ‘A%’;

SELECT E1.ENAME, E2.ENAME “ MANAGER NAME” FROM EMP E1, EMP E2
WHERE E1.MGR = E2.EMPNO;
employee ‘KING’.


SELECT ENAME, JOB FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM
EMP WHERE ENAME = ‘KING’);
 
 
SELECT ENAME, DNAME FROM EMP E, DEPT D WHERE E.DEPTNO = D.DEPTNO;
earnings (sal+comm) of each employee.
 
 
SELECT SAL + NVL(COMM,0) AS “NET EARINING” FROM EMP;


SELECT ENAME FROM EMP WHERE ENAME LIKE ‘M%’;
 
 
SELECT ENAME,HIREDATE FROM EMP WHERE HIREDATE LIKE ‘%FEB%’;
 
 
SELECT ENAME FROM EMP WHERE FLOOR((SYSDATE-HIREDATE)/365)>12;
 
 
SELECT INITCAP(ENAME) FROM EMP;
 
 
SELECT SUBSTR(ENAME,1,3) FROM EMP;
 
 
SELECT REPLACE(ENAME,’A’,’a’) FROM EMP;
 
 
SELECT SAL, ROUND(SAL/1000)*1000 FROM EMP;


SELECT COUNT(*) FROM EMP WHERE JOB = ‘CLERK’ AND HIREDATE >’01-JAN81’;
 
 
SELECT E2.ENAME FROM EMP E1, EMP E2 WHERE E1.MGR = E2.EMPNO AND
E1.DEPTNO !=30;
headings as NUMBER and NAME.


SELECT ENAME “NAME”, EMPNO “NUMBER” FROM EMP;


SELECT ENAME FROM EMP WHERE COMM IS NULL;


SELECT ENAME FROM EMP WHERE ENAME LIKE ‘%M’;


SELECT ENAME FROM EMP WHERE ENAME LIKE ‘_ _R%’;


SELECT ENAME FROM EMP WHERE HIREDATE IN LAST_DAY(HIREDATE);
 
 
SELECT IPAD(ENAME,'15',' ') “ NAME” FROM EMP;


SELECT ENAME,INSTR(ENAME,’AR’,1,1) FROM EMP;
 
 
SELECT ENAME FROM EMP WHERE HIREDATE LIKE ‘%FEB%’;
 
 
SELECT ENAME FROM EMP WHERE SAL = (SELECT MAX(SAL) FROM EMP);
 
 
SELECT MOD(9,5) FROM DUAL;
 
 
SELECT HIREDATE,ROUND( MONTHS_BETWEEN(SYSDATE, HIREDATE))
“MONTHS” FROM EMP;
 
 
SELECT TO_CHAR(SYSDATE,’DAYMONTHDD, YYYY’) FROM DUAL;
 
 
SELECT SUM(SAL) FROM EMP WHERE DEPTNO=10;


SELECT MIN(SAL), JOB FROM EMP GROUP BY JOB;
 
 
SELECT SAL*20/100 “HRA” FROM EMP;
 
 
SELECT DISTINCT(DEPTNO) FROM EMP;
 
 
SELECT * FROM EMP WHERE JOB LIKE ‘%T’;
be displayed even if a particular DEPTNO has no employees.
 
SELECT E.ENAME, D.DNAME FROM EMP E, DEPT D WHERE E.DEPTNO =
D.DEPTNO;
 
 
SELECT ENAME, SAL FROM EMP WHERE SAL=(SELECT MIN(SAL) FROM EMP)